<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Taskr</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <!-- Bootstrap -->
    <link href="css/bootstrap.min.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
  </head>

  <body class="container">

    <nav class="navbar navbar-inverse">
      <div class="container-fluid">
        <div class="navbar-header">
          <a class="navbar-brand" href="#">taskr</a>
        </div>
        <ul class="nav navbar-nav">
          <% if !current_user %>
            <li class="active"><%= link_to "Sign In", signin_path %></li>
            <li class="active"><%= link_to "Sign Up", new_user_path %></li>
          <% else %>
          <li><%= link_to "Ma P-file", user_path(current_user) %></li>
          <li><%= link_to "Sign Out", signout_path, method: :delete %></li>
          <% end %>
        </ul>
        <button class="btn btn-danger navbar-btn">Button</button>
      </div>
    </nav>

    <button class="btn btn-danger navbar-btn"><%= link_to "All Events", events_path %></button>
    <button class="btn btn-danger navbar-btn"><%= link_to "All Tasks", tasks_path %></button>
    <button class="btn btn-danger navbar-btn"><%= link_to "Home Page", root_path %></button>

    <%= yield %>

  <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->

  <!-- Include all compiled plugins (below), or include individual files as needed -->
  <script src="js/bootstrap.min.js"></script>
  </body>

</html>
